name: Issue Hygiene

on:
  issues:
    types: [opened, edited]

permissions:
  contents: read

jobs:
  acknowledge:
    runs-on: ubuntu-latest

    steps:
      - name: Record issue event
        run: echo "Issue event received for repository triage."
